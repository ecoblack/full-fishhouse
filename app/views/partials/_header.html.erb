 <nav class="navbar navbar-inverse" style="margin-bot:15%!important;padding-top:10px!important;">
  <div class="container-fluid" >
    <div class="navbar-header" >
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>                        
      </button>
      <a  class="navbar-brand" href="/" style="text-decoration:none; color:#00ccff; margin-left:-5%; white-space: nowrap;" ><%= image_tag image_url('logo_mini.png'), class: 'img-responsive', size: "55", style:  "margin-left:25px;margin-top:-10%;display-inline:block;" %>Full Fish House</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
       
 




        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">Продукция <span class="caret"></span></a>
          <ul class="dropdown-menu" style="margin-left:4%;">
          
                <li class="active">
                
                      <% Category.where(parent_id: 1).each do |category| %>
                 <li> <%= link_to category.name, category_path(category) %></li> 
                    <% end %> 
                  <hr>

                  
                     <% Category.where.not(parent_id: 1).each do |category| %>
                   <li> <%= link_to category.name, category_path(category) %>
                    <% end %> 
                   </li> 
                   
                    </ul>
        
             
             <li><%= link_to "Контакты", contact_path %></li>
             <li><%= link_to "О нас", about_path %></li>
               
        </ul>

<!-- Navbar search form rails-->
<%= form_tag('/search', local: true, method: :get, class: 'form-inline') do %>
  <%= text_field_tag(:q, nil, data: { behavior: 'autocomplete' }, placeholder: 'Поиск', class: 'form-control mr-sm-2') %>
     <%= button_tag(class: "btn btn-info btn-lg", type: "submit", name: nil) do %>
              <i class="glyphicon glyphicon-search"></i>
              <% end %>
<% end %>


    </div>
  </div>
</nav>
  
      
